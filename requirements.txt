requests
beautifulsoup4
cloudscraper
lxml
tqdm
Flask
py7zr
rarfile
python-dotenv
discord.py>=2.4.0

# yt-dlp / gallery-dl are intentionally UNPINNED — downloaders must track upstream
# latest (site extractors break constantly; pinning would freeze in known-broken
# versions). Updates happen reactively on a "stale extractor" failure or manually
# via the Web UI / `-U` launcher flag — see app/config/downloaders.py and
# app/services/updater_service.py. Do NOT pin these.
yt-dlp
gallery-dl