# Dev/quality-gate tooling ONLY — never installed in production. Pinned exact versions per
# cluster rebuild-pinning rule (a rebuild that re-resolves these to "latest" can silently
# change gate behavior/output shape without anyone deciding that).
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.12
mypy==1.20.2
import-linter==2.13
diff-cover==10.5.1
