# 站主裁定紀錄 — 2026-09-05

repo-scoped 裁定，無對應 blueprint feature entry（quality-gates 屬開發工具鏈/CI 基礎設施，
非 blueprint 定義的「使用者關心的最終功能」，故記於此而非 `docs/blueprint/entries/`）。

## fix/guard-ordering 分支

- **context**：`fix/guard-ordering` 分支修正 G1/G2/G4 baseline 檢查器（
  `quality-gates/check_ruff_baseline.py`／`check_mypy_baseline.py`／`check_import_cycles.py`）
  的 new-vs-resolved 判斷順序 fail-open 缺陷（純測 run 只報 resolved 不報 new；
  `--update-baseline` 無條件寫檔不核對 new）。
- **PM 提問**：分支是否已完成、可否合併？
- **站主原話**：「做完阿 問啥低能問題」
- **resolution**：已完成，merged `main` @ `5007b23`（merge commit
  "merge: fix/guard-ordering → main — close new-vs-resolved ordering fail-open in
  G1/G2/G4 baseline checkers"）。機制、缺陷細節與重現證據見
  `D:/backup/CSIA/.ns-media-hub/.claude/CLAUDE.md` 「G1/G2/G4 guard-ordering fix」一節。
- **date**: 2026-09-05（記錄日；裁定原始日期依 commit `5007b23` 時間戳為 2026-09-01）
